<!DOCTYPE html>
<html>
<head>
  <title>Trabajadores</title>
</head>
<body>
<header>
     <h1 style="padding-left: 30px;">Lista de direcciones de trabajadores</h1>
</header>
<section style="padding:20px;">
<table width="70%"  border="1" >
  <thead>
    <tr bgcolor="lightblue" >
      <th>Calle</th>
      <th>Numero</th>
      <th>Comuna</th>
      <th>Ciudad</th>
      <th>Region</th>
      <th>Pais</th>
      <th>Trabajador  </th>
      <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
    <% @direccions.each do |direccion| %>
      <tr >
        <td><%= direccion.calle %></td>
        <td><%= direccion.numero %></td>
        <td><%= direccion.comuna %></td>
        <td><%= direccion.ciudad %></td>
        <td><%= direccion.region %></td>
        <td><%= direccion.pais %></td>
        <td><%= direccion.trabajador.nombres %></td>
        <td><%= link_to 'Mostrar', direccion %></td>
        <td><%= link_to 'Editar', edit_direccion_path(direccion) %></td>
        <td><%= link_to 'Eliminar', direccion, method: :delete, data: { confirm: 'Atencion! Esta a punto de eliminar el registro seleccionado, desea continuar de todas formas?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>
<%= link_to 'Crear nuevo Registro', new_direccion_path %>
</section>


</body>
</html>
